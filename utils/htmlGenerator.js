const fs = require ('fs');

const generatePage = ({
    name,
    position,
    identification,
    email,
}) => {
    console.log('GENERATING HTML PAGE');
    return (
        // input HTML
        `
    
        `
    )
}